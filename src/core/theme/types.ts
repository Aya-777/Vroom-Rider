export type ThemeColors = {
    background: string;
    backgroundSoft: string;

    surface: string;
    surfaceAccent: string;

    textPrimary: string;
    textSecondary: string;
    textMuted: string;

    primary: string;

    border: string;

    neutral: string;
};